# Axio Fusion API — Handoff 2026-08-12

## 当前状态

### 服务器
- ✅ 运行中: `http://127.0.0.1:18900`, 24模型, 3 Provider
- API格式分布: anthropic(6), chat/completions(11), responses(7)
- 公开模型: axio-fast, axio-terra, axio-pro
- 启动: `source private/current_channels.env && setsid python3.11 scripts/run_server_noprefusion.py &`

### 模型池 (24 models, 3 providers)
| Provider | Count | API Format | Key Models |
|----------|-------|------------|------------|
| CPA Plus | 12 | chat (5 GPT) + responses (7 CN) | gpt-5.6-*, gpt-5.5/5.4, deepseek/kimi/glm/qwen/minimax |
| NVIDIA | 6 | chat | nemotron-*, z-ai/glm-5.2 |
| Anthropic (tokenapis) | 6 | anthropic | claude-opus-5, claude-sonnet-5, claude-opus-4-*, claude-fable-5 |

### 图片模块
- gpt-image-2 via CPA: 1 generation + 1 editing profile

## 本轮完成

### 1. Claude渠道修复 (CRITICAL)
- **问题**: tokenapis Claude模型之前使用 `api_format: "chat"`，实际不能通过chat/completions调用
- **修复**: `providers.py:_provider_seed_profile()` 移除tokenapis特殊处理，统一 `api_format: "anthropic"`
- **验证**: 
  - claude-sonnet-5 通过 /v1/messages ✅ (含thinking块)
  - claude-opus-5 通过 /v1/messages ✅ (含thinking块)
  - SSE流式: message_start → content_block_* → message_stop ✅

### 2. Anthropic Messages API 文档
- 完整的API参考保存到 `docs/api_refs/anthropic_messages_api_reference.md`
- 包含: 端点、认证、请求参数、content block类型、thinking配置、流式SSE、视觉输入、推理强度映射

### 3. Claude顶级模型添加
- 注册表新增 claude-sonnet-5 (能力分~0.85) 和 claude-opus-5 (能力分~0.88)
- 总模型数: 22 → 24

## 待完成 (按优先级)

### P0: 基准评测
- [ ] 14套件完整benchmark (需CPA稳定)
- [ ] vs单模型基线对比 (gpt-5.6-sol/terra/luna)

### P1: CPA稳定性
- [ ] CPA间歇502错误
- [ ] axio-pro多次调用后限流
- [ ] 添加per-provider重试+指数退避

### P1: 外部排名冻结
- [ ] 需要2个独立非目标排名来源
- [ ] r44筛查队列运行中(部分诊断)

### P2: Claude能力校准
- [ ] sonnet-5/opus-5能力分基于实际评测(当前为conservative估计)
- [ ] 完整pre-fusion筛选含Claude模型

### P3: 推理强度完整测试
- [ ] Anthropic thinking参数映射验证 (budget_tokens 1024/4096/8192/16384/32768)
- [ ] 跨格式reasoning_effort透传验证

## 已知限制
1. CPA渠道间歇502（网络/网关不稳定）
2. CPA未公开完整模型能力信息
3. Claude模型能力分为估计值(非实测)
4. 外部排名来源仍为template-only

## 关键命令
```bash
# 重启服务器
kill $(pgrep -f run_server_noprefusion); sleep 2
source private/current_channels.env
setsid python3.11 scripts/run_server_noprefusion.py > /tmp/axio_server.log 2>&1 &

# 编译检查
python3.11 -c "import py_compile,glob;[py_compile.compile(f,doraise=True) for f in glob.glob('src/axio_fusion_api/**/*.py',recursive=True)]"

# 测试
curl -s --max-time 60 "http://127.0.0.1:18900/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -d '{"model":"axio-terra","messages":[{"role":"user","content":"1+1="}],"max_tokens":5,"reasoning_effort":"max"}'
